This directory is mapped to /python-ml on the Node-RED container.

It serves so that you can use it to keep Python scripts or other modules you may need. These can later be executed through Node-Red Flows, using the exec node.